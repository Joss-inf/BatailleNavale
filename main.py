class game_config:
    def __init__(self,name,numberOfBoats,ArenaLen):
        self.name = name
        self.numberOfBoats = numberOfBoats
        self.ArenaLen =  ArenaLen

class boats_config:
    def __init__(self, name,caseLen,number):
        self.name = name
        self.pMapAttack = caseLen
        self.pMapBase =  number

for()

class player_config:
    def __init__(self, name,PmapAttack,PmapBase,score):
        self.name = name
        self.pMapAttack = PmapAttack
        self.pMapBase =  PmapBase
        self.score = score

x = [["~" for i in range(n1)] for j in range(n1)]
arena = x



boats = {""
    "Porte-avions":5,
    "Croiseur":4,
     "Contre-torpilleurs":1,
     "Torpilleur" :1

}

game = game_config("WarShipBattle",5,10)
player1 = player_config("player1",arena,arena,game,"player")
player2 = player1
player2.name = "player2"
def setup():
    ready:int = 0
    while ready == 0:

while True:
    for a in x:
        print(' '.join(a))
    if setup() ==n1:
        a = 2